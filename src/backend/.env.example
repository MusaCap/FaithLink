# Database Configuration
DATABASE_URL="postgresql://username:password@hostname:5432/database_name"

# JWT Security
JWT_SECRET="your-super-secret-jwt-key-at-least-32-characters-long"

# Email Configuration (for bug reports)
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Environment
NODE_ENV="development"
PORT=8000

# Instructions:
# 1. Copy this file to .env
# 2. Replace DATABASE_URL with your actual PostgreSQL connection string from Render
# 3. Generate a secure JWT_SECRET (at least 32 characters)
# 4. Add your SMTP credentials for bug report emails to gp@musa.capital
